-- Create the database for the Hibernate Delivery HQL demo
CREATE DATABASE fsadendexam;

-- Optional: grant privileges if you want a dedicated user
-- CREATE USER 'fsaduser'@'localhost' IDENTIFIED BY 'yourpassword';
-- GRANT ALL PRIVILEGES ON fsadendexam.* TO 'fsaduser'@'localhost';
-- FLUSH PRIVILEGES;
